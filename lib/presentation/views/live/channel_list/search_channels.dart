import 'package:flutter/material.dart';

class SearchChannels extends StatelessWidget {
  const SearchChannels({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
