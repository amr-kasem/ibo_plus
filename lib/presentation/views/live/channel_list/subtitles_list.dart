import 'package:flutter/material.dart';

class SubtitlesList extends StatelessWidget {
  const SubtitlesList({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
